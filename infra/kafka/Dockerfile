# kafka 커스텀 이미지 — KRaft 단일 노드(ZooKeeper 없음), 브로커 설정은 compose 환경변수로.
# 공식 apache/kafka 이미지 사용(Confluent·Bitnami 배제 — ADR-0009 결정 4).
# 버전은 안정판 정책: 2026-07 기준 최신 4.3.1에서 두 마이너 뒤인 4.1.x(패치 축적).
FROM apache/kafka:4.1.2
