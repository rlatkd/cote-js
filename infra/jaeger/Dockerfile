# Jaeger v2(OTel Collector 기반 단일 바이너리) — 분산 추적 백엔드 + UI.
# 개발용 all-in-one: 수집(OTLP)·저장(메모리)·조회(UI :16686)를 한 컨테이너가 담당.
# 버전은 안정판 정책(최신 2.20.0 직전의 2.19.0) — 근거는 ADR-0018.
FROM jaegertracing/jaeger:2.19.0
