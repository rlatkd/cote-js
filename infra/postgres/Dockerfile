# postgres 커스텀 이미지 — 인프라 서비스도 서비스별 Dockerfile을 갖는다(사용자 지시).
# 현재는 베이스 그대로. M4(유사도)에서 pgvector 확장을 여기서 설치한다.
#   (예정) FROM pgvector/pgvector:pg16  또는  RUN apk add ... && make install
FROM postgres:16-alpine
